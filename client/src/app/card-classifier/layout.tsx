// src/app/login/layout.tsx
import React from 'react';

const CardClassifierLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <>
        {children}
      </>
  );
};

export default CardClassifierLayout;